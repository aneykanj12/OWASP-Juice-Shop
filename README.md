# OWASP-Juice-Shop

## Description

This is my Part 2 Solution for the OWASP Juice Shop Assignment in which I attempted to replicate the functionality behind the login page and behavior from the website. The website has a client side and server side for the basic frontend UI and backend functionality. All the files are contained on this GitHub repository. It is important to note that the objectives to implement from the assignment description are in the backend, all supported with basic JavaScript, Node.js, and Express.js for basic function. The frontend follows a vanilla UI, as I focused more on the functionality rather than the appearance.

## How to Use

1. Begin with running the command:
```git clone https://github.com/aneykanj12/OWASP-Juice-Shop.git```
2. Open up the directory in a terminal and run:
```cd server_side/```
3. Next install npm if not already installed:
```npm install```
4. Finally launch the page using the following command and open via localhost when prompted:
```npm start```
